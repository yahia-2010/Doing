import React from "react";
import { IoClose } from "react-icons/io5";

const CloseButton = () => {
  return (
    <button>
      <IoClose size={30} />
    </button>
  );
};

export default CloseButton;
